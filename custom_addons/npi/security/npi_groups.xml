<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Satu grup akses untuk modul NPI. Admin otomatis masuk. -->
    <record id="group_npi_user" model="res.groups">
        <field name="name">Akses NPI</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="user_ids" eval="[(4, ref('base.user_admin'))]"/>
    </record>
</odoo>
